schema_version = "1.0.0"

id = "blendswap_browser"
version = "0.2.1"
name = "Blend Swap Browser"
tagline = "Search, download and import Blend Swap assets"
maintainer = "Jace Whitten"
type = "add-on"

website = "https://github.com/jacebot/blendswap-browser"

tags = ["Import-Export"]

blender_version_min = "4.2.0"

license = ["SPDX:GPL-3.0-or-later"]

# The add-on talks to the Blend Swap REST API to search and fetch assets.
[permissions]
network = "Search and download assets from the Blend Swap API"
